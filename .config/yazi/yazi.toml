[opener]
edit = [
    { run = 'nvim "$@"', block = true, desc = "Neovim" }
]

ksnip = [
    { run = 'ksnip "$@"', orphan = true, desc = "Open in Ksnip" }
]

[open]
rules = [
    { mime = "image/*", use = "ksnip" },
    { mime = "text/*", use = "edit" },
]
